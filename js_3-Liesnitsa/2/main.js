function ask(n) {
    for (let i = 2; i <= n; i++) {
        let n = +prompt('Введіть число')
        console.log('Це просте число')
            // for (let j = 2; j < i; j++) {
            //     if (i % j == 0);
            // }
    }
}
ask(n)